function_A는 O(n)의 시간복잡도를 가진다.
for i in range(2, n)을 통해 n번에 근사하는 연산을 수행하기 때문이다.

function_B는 O(n)의 시간복잡도를 가진다.
for i in range(3, n, 2)를 통해 n/2번에 근사하는 연산을 수행하기 때문이다.

function_C는 O(sqrt(n))의 시간복잡도를 가진다.
while (index*index <= n)을 통해 sqrt(n)번에 근사하는 연산을 수행하기 때문이다.